/**
 * Public contact addresses.
 *
 * ⚠️ These mailboxes must actually exist and be monitored — a contact page that bounces is
 * worse than no contact page. `privacy@` is the one already published (in the Privacy Policy);
 * `hello@` is new and needs creating, or aliasing onto an inbox you read.
 */
export const SUPPORT_EMAIL = 'hello@fateround.com'
export const PRIVACY_EMAIL = 'privacy@fateround.com'
/** Questions about the Terms of Service. */
export const TERMS_EMAIL = 'terms@fateround.com'
/** Content reports / abuse. Shares the general inbox for now. */
export const LEGAL_EMAIL = SUPPORT_EMAIL

export const SOCIAL_LINKS = {
  tiktok: 'https://www.tiktok.com/@fateround',
  x: 'https://x.com/Fateround',
} as const
